server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AideWorld/cloud2020-config.git
          search-paths:
          - cloud2020-config

eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/